<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.cqupt.mislab.excellentroom.dao.ProjectIntroduceDao">
    <resultMap id="ProjectIntroduce" type="cn.edu.cqupt.mislab.excellentroom.domain.entity.ProjectIntroduce">
        <result property="projectId" column="projectId" jdbcType="VARCHAR"/>
        <result property="projectIntroduceBkgroundUrl" column="projectIntroduceBkground" jdbcType="VARCHAR"/>
        <result property="projectIntroduceText" column="projectIntroduceText" jdbcType="VARCHAR"/>
        <result property="projectIntroduceTitle" column="projectIntroduceTitle" jdbcType="VARCHAR"/>
    </resultMap>

    <update id="updateProjectIntroduceBkgroundUrl">
        update t_project_information
        set projectIntroduceBkground = #{projectIntroduceBkgroundUrl}
        where projectId = #{projectId}
    </update>

    <select id="selectProjectIntroduceBkgroundById" resultMap="ProjectIntroduce" resultType="cn.edu.cqupt.mislab.excellentroom.domain.entity.ProjectIntroduce">
        select projectIntroduceBkground,projectId
        from t_project_information
        where projectId = #{projectId}
    </select>

    <update id="updateProjectIntroduceText">
        update t_project_information
        set projectIntroduceText = #{projectIntroduceText}
        where projectId = #{projectId}
    </update>

    <select id="selectProjectIntroduceTextById" resultMap="ProjectIntroduce" resultType="cn.edu.cqupt.mislab.excellentroom.domain.entity.ProjectIntroduce">
        select projectIntroduceText,projectId
        from t_project_information
        where projectId = #{projectId}
    </select>

    <update id="updateProjectIntroduceTitle">
        update t_project_information
        set projectIntroduceTitle = #{projectIntroduceTitle}
        where projectId = #{projectId}
    </update>

    <select id="selectProjectIntroduceTitleById" resultMap="ProjectIntroduce" resultType="cn.edu.cqupt.mislab.excellentroom.domain.entity.ProjectIntroduce">
        select projectIntroduceTitle,projectId
        from t_project_information
        where projectId = #{projectId}
    </select>



</mapper>